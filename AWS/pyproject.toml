[project]
name = "cv-aws-cloud-sizing"
version = "0.1.0"
description = "AWS cloud resource discovery / sizing tool (Commvault protection planning)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.34.0",
]
